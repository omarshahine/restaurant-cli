# Append these to ~/.secrets.env (never commit the real values).
# `restaurant setup <provider>` can do this for you interactively.

export RESY_API_TOKEN="paste-your-token-here"
# export OPENTABLE_API_TOKEN="..."
# export TOCK_API_TOKEN="..."
